# Shadow monorepo parent [patch.crates-io] for chunk-your-skills (see ../.cargo/config.toml).
# Overriding the crate to its own manifest path prevents [[patch.unused]] churn in
# Cargo.lock when parent sync-version bumps versioned worktree paths.
[patch.crates-io]
chunk-your-skills = { path = "." }
